package com.web.audio.entity;

/**
 * Interface {@code Identifiable} is
 * an interface marker for objects of entity.
 */
public interface Identifiable {

    /**
     *
     * @return implements this interface object id
     */
    int getId();
}
